#program that takes a string as input and returns its length
str = input("Enter string: ")
len = 0
for i in str:
    len +=1
print("Length of string: ", len)
''' There also exists a simple 'len(str_name)' function in Python that returns the length of the string passed as a parameter.'''
